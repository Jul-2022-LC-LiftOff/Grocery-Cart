import { RecipeIngredientDto } from './recipe-ingredient-dto';

describe('RecipeIngredientDto', () => {
  it('should create an instance', () => {
    expect(new RecipeIngredientDto()).toBeTruthy();
  });
});
